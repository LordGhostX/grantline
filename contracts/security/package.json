{
  "name": "grantline-contract-tools",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "lint:solhint": "cd .. && solhint 'src/**/*.sol' --config security/.solhint.json --ignore-path security/.solhintignore --quiet"
  },
  "devDependencies": {
    "solhint": "6.2.4"
  }
}
